key = value           # Invalid if enable_default_section == False

[]                    # Invalid if enable_default_section == False or allow_section_split == False
key   = another value # Attempt to replace a key implicitly (invalid if set_is_replace == False)
empty =               # Invalid if allow_empty_values == False
